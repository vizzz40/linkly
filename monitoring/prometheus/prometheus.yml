global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: linkly
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8000"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
