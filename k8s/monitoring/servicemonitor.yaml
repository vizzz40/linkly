apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: linkly
  namespace: linkly
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: linkly
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
